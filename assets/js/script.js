// shows current date in day, month, number
var currentDay = moment().format("dddd, MMMM Do");
$("#currentDay").text(currentDay);

var currentTime = moment().hours()



// if statements for time, if current time then one color, 
// if past another if future another color
// css classes are .hour .past .present .future


//moment().hours()


// save button = local storage

// id for each time